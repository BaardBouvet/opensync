{
  "connectors": {
    "crm": {
      "plugin": "../../../connectors/mock-crm/src/index.ts",
      "auth": {
        "apiKey": "${MOCK_CRM_API_KEY}"
      },
      "config": {
        "baseUrl": "http://localhost:4001",
        "webhookMode": "thick"
      }
    },
    "erp": {
      "plugin": "../../../connectors/mock-erp/src/index.ts",
      "auth": {
        "clientId": "opensync-test",
        "clientSecret": "secret"
      },
      "config": {
        "baseUrl": "http://localhost:4002"
      }
    }
  }
}
