{
  "name": "opensync",
  "private": true,
  "workspaces": [
    "packages/*",
    "connectors/*",
    "servers/*"
  ],
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --build --dry",
    "test": "bun test"
  },
  "dependencies": {
    "js-yaml": "^4.1.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9"
  }
}
