{
  "name": "opensync",
  "private": true,
  "workspaces": [
    "packages/*",
    "connectors/*"
  ],
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --build --dry",
    "test": "bun test"
  }
}
